{
  "mcpServers": {
    "fetcher": {
      "command": "cmd.exe",
      "args": ["/c", "npx", "-y", "fetcher-mcp"],
      "description": "웹 콘텐츠 추출 (TikTok API 문서 참조)"
    },
    "context7": {
      "command": "cmd.exe",
      "args": ["/c", "npx", "-y", "@upstash/context7-mcp"],
      "description": "최신 라이브러리 문서 (Fastify, Vitest 등)"
    }
  }
}
